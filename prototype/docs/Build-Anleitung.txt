
-- DebianLinux-System zum Laufen bringen --

    - Oracle VirtualBox installieren
    - Maschine erzeugen
        Maschine > Neu >
        Linux, Debian64 auswÃ¤hlen
        Speichergröße passend festlegen
        vorhandene Festplatte verwenden, PTJ.vdi im mitgelieferten Image auswählen
        jetzt ist die Maschine erzeugt
    -  In den globalen Einstellung von Virtualbox sicherstellen, dass unter Netzwerk
    der HostOnlyAdapter vboxnet0 hinzugefügt ist (+ Symbol, Defaults bestätigen)
    - Der Hostonly Adapter muss unter der Konfigurationd es eben hinzgefügten Images 
    unter Adapter 2 hinzugefügt sein.
    - Adapter 1 soll der NAT Adapter sein.
    - In der folgenden Ausführung wird die IP 192.168.56.102 als ReferenzIP angeben.
    U.u. mappt Virtualbox die IP auch auf [..].101
    - Das System ist nun Betriebsbereit

----------------

-- Linux-Login für das Projekt --

username:   ptj
pass:       ptj

Das Projektverzeichnis mit vorkonfiguriertem GIT + Maven Projekt befindet sich unter
/home/ptj/sp1-plan-the-jam/prototype/de.bht.comanche

--------------

-- Build-Prozess --

mvn package 
im Projektverzeichnis.


---------------
-- Applikation ausführen -- 

Server starten:

mvn jetty:run

Der Zugriff auf die Applikation ist im lokalen Browser (Hostsystem) mit Hilfe der IP
    192.168.56.102:8080
und im Image (Gastsystem) unter
    localhost:8080
möglich.

 
--------------

-- Fernzugriff --

ssh ptj@192.168.56.102
pass: ptj 

--------------

-- Eclipse-IDE anbinden --

- lokal Eclipse Enterprise Edition installieren
- sshfs installieren
    - für Windows muss zuvor die Dokan-Library installiert werden -> DokanInstall_0.6.0.exe
    - Daten für die sshfs-Installation:
        - beliebigen Drive-Namen vergeben
        - Host: 192.168.56.102
        - Port: 22
        - Username: ptj
        - Pass: ptj
        - Directory: /home/ptj
- Drive mounten
- in Eclipse Maven-Projekt vom gemounteten Drive importieren

--------------

-- Postgres-Datenbank mit pgAdmin-III anzeigen --

- pgAdmin ist im Image vorinstalliert. Nach einloggen mit User PTJ unter
Anwendungen/Entwicklung zu finden.

- Vom Host-System & lokal installiertem Client mit VirtualBox-IP statt localhost ansprechbar.

- Login-Daten (Gastsystem):
    Name:           <name>
    Host:           'localhost'
    Port:           '5432'
    Service:        ''
    Wartungs-DB:    'ptjdb'
    Benutzername:   'postgres'
    Passwort:       'postgres'
